# workflows

This directory contains workflow files that expected to be used in other GitHub Actions workflows via `workflow_call` event.
The location of this directory is important, and it must be placed under `.github/workflows/`.
